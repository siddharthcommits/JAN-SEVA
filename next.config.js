/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // appDir is the default with the app router; remove unsupported experimental key
}

module.exports = nextConfig
